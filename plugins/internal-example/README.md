# Internal Example Plugin

Template plugin for Antigravity OS internal extensions.

- `commands/` for operator commands
- `agents/` for specialist agents
- `skills/` for reusable skills
- `.mcp.json` for MCP configuration
